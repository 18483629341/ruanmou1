import { Component, Input, Output} from '@angular/core';

interface Hero {
  id: number;
  name: string;
}
@Component({
  selector: 'app-lunbo',
  template: `
      <p>轮播</p>
  `,
})
export class LunboComponent {
   
}